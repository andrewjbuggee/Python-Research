# Extra dependencies for the ERA5 observation-feedback check. Kept separate
# from the project's main environment because ecmwf-api-client and pyodc are
# only needed for this one question, and neither is on conda-forge by default.
#
#   pip install -r requirements-odb.txt
#
pandas>=1.5
numpy>=1.23
pyodc>=1.3          # pure-Python ODB-2 reader; no ODB C library required
ecmwf-api-client>=1.6.3   # only needed if driving MARS remotely rather than
                          # running the mars CLI on an ECMWF platform
